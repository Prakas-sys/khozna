{
  "name": "khozna-root",
  "private": true,
  "scripts": {
    "build": "npm install --prefix admin_dashboard_web && npm run build --prefix admin_dashboard_web"
  },
  "dependencies": {
    "pg": "^8.22.0"
  }
}
