import 'package:flutter/material.dart';

class BoostPromotionScreen extends StatelessWidget {
  const BoostPromotionScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Boost Promotion')),
      body: const Center(child: Text('Boost Promotion feature coming soon!')),
    );
  }
}
