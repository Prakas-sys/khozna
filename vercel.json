{
  "version": 2,
  "buildCommand": "npm install --prefix admin_dashboard_web && npm run build --prefix admin_dashboard_web",
  "outputDirectory": "admin_dashboard_web/dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
