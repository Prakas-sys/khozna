{
  "buildCommand": "cd admin_dashboard_web && npm install && npm run build",
  "outputDirectory": "admin_dashboard_web/dist",
  "installCommand": "echo 'skip root install'",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
